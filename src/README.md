
 - What is JSX, and why is it used?

  JavaScript এর ভিতরে HTML-এর মতো code লিখতে দেয়,JSX ব্যবহার করলে HTML-এর মতো কোড পড়া ও লেখা সহজ।
 - What is the difference between State and Props?

  State= useState দিয়ে তৈরি করা হয়,Props=Parent component থেকে pass করা হয়
 - What is the useState hook, and how does it work?

  useState এর মাধ্যমে component এর data store করা এবং update করা যায়, [count বর্তমান  value,setCount update করে]=useState(0)
 - How can you share state between components in React?

  State কে parent component এ রাখা হয়, তারপর props দিয়ে child components এ পাঠানো হয়।
 - How is event handling done in React?
    onClick - event name={handleClick}eventকরার function